name: lik-pylate-only
resources:
  infra: kubernetes
  accelerators: H100:1
workdir: .
run: |
  set -eux
  cd ~/sky_workdir
  pip install --no-cache-dir -e ".[dev,pylate]" --force-reinstall --no-deps >/dev/null 2>&1
  pytest tests/test_pylate_compat.py -v
